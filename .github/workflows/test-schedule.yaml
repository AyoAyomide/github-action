name: Check action cron
on:
  schedule:
    - cron: "2/5 * * * *"

env:
  Name: "sharesquare"

jobs:
  RUN-CRON:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo time
        run: echo running action @ {{ github.event.schedule }}
